-r requirements.txt

# Install PyTorch separately first so pip selects the correct CPU build for
# macOS, Windows, or Linux. Then install this file.
transformers==4.45.2
safetensors>=0.5,<1
numpy>=1.26,<3
pyab3p>=0.1.1,<1
huggingface-hub>=0.24,<1
